enum SyncStatus { pending, synced, conflicted, failed }

enum TransactionDirection { inflow, outflow }

enum BudgetMode { monthly, weekly, custom }

enum AccountType { checking, savings, credit, cash, investment }

enum DebtType { creditCard, loan, mortgage, other }

enum GoalStatus { active, completed, paused }

enum RecurringFrequency { daily, weekly, monthly, yearly }
